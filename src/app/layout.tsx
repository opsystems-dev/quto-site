import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Quto - AI-Powered Asset Management for Construction",
  description: "Finally, fault reports that actually help. Our AI assistant turns useless complaints into actionable details, plus proper maintenance tracking and compliance.",
  keywords: "construction asset management, plant management, AI fault diagnosis, maintenance tracking",
  icons: {
    icon: "/ops-logo-colour.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
